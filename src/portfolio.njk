---
layout: body
eleventyNavigation:
    key: Portfolio
---
<h2 class='text-center mt-20'>Portfolio</h2>
<div class='flex flex-wrap justify-center gap-1 mb-10'>
    {# <div class='grid grid-cols-2 gap-1'> #}
    {% for category in categories.data %}
        <a href="{{ category.name | slug }}" class="btn">{{category.name}}</a>
    {% endfor %}
</div>
<div class="flex flex-col">
    {% for project in collections.projects %}
        {% include "partials/project-tile.njk" %}
    {% endfor %}
</div>
